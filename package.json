{
  "name": "construction-career-survey",
  "version": "1.0.0",
  "description": "Construction Career Explorer Survey with Firebase Integration",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "start": "serve -s .",
    "build": "echo 'No build step required for static files'",
    "dev": "serve -s . -l 3000",
    "deploy": "git push render main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "construction",
    "career",
    "survey",
    "firebase"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "firebase": "^11.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
